# Changelog

## 33
База: 26.zip (последняя «стабильная» версия до экспериментов с Fix.zip — Fix.zip в расчёт не берём).

### Изменения
- Фоны:
  - Заменён фон шапки (desktop + mobile) на версии с более плавным переходом к траве.
  - Фон страницы после шапки переведён на «траву» без видимых швов/полос при скролле (подготовлен тайл + repeat-y).
  - На внутренних страницах (Сборы/Шахматы/Родителям/Кабинет и т.д.) шапка использует фон травы, чтобы не было резкой границы «шапка → фон».
- Родителям:
  - Усилен blur и читаемость в дропдауне (desktop) и в мобильном всплывающем окне.
- Личный кабинет:
  - Полностью убран блок «Что будет внутри» и лишняя кнопка «Локации».
  - Форма входа выровнена по центру (desktop + mobile).
- Цены:
  - Шаблонные карточки «ОФП» и «Теоретические занятия» приведены к единому формату (без `undefined`).
  - Добавлен ещё один шаблонный блок: «Шахматы».
- Квиз:
  - Выбор «Возраст» и «Район» открывается поповером, привязанным к кнопке (не «листом» снизу), с аккуратной анимацией.

### Медиа
- Вставлены тестовые фото тренеров: `assets/img/coaches/coach-1.png`, `assets/img/coaches/coach-2.png`.

## 34
### Исправления после теста 33
- Quiz: исправлен критический баг в `assets/js/quiz.js` (лишний дублирующий блок кода ломал логику выбора возраста/района).
- Родителям (desktop): меню больше не “прячется” при попытке навести/кликнуть (добавлен hover-bridge + аккуратная стрелка-указатель).
- Родителям → «Программа тренировок»: ссылка ведёт на `index.html#program` (раздел программы на сайте).
- Мобильные карточки тренеров/выпускников: убрано клипание карточек в карусели (на mobile `overflow: visible` у carousel-inner).
- Фон под шапкой: добавлены переменные прозрачности “плёнки” в `assets/css/theme.css` и по умолчанию плёнка отключена (включается настройкой переменных).

## 35
### Правки по итогам теста 34
- Личный кабинет (desktop): футер теперь “прилипает” к низу экрана на короткой странице (sticky footer только для `data-page="cabinet"`).
- Фон тела сайта: убраны “стыки/кусочки” — фон теперь без повторов (cover) + отдельная регулируемая “плёнка” через `body:before`.
- Переход “шапка → тело”: добавлен плавный blend через `mask-image` у hero-фона (меньше резкий стык).
- theme.css: добавлена памятка-комментарий, как настраивать прозрачность “плёнки” (примеры значений).
- Mobile “Родителям” → “Буллинг”: исправлено открытие страницы (ссылки в модалке закрывают окно и гарантированно переходят по href).


## 36 (2026-02-07)
- Фон тела сайта: заменён на “Tekstura” (webp) и сделан плиткой (`repeat`) без заметных “стыков”; добавлены переменные `--site-bg-size-desktop` / `--site-bg-size-mobile` для тонкой настройки масштаба.
- Quiz: возраст теперь от **3** до 15 лет + 16+.
- Quiz: выбор возраста/района на desktop теперь открывается **по центру** (без неудобного скролла по одному пункту), варианты показаны сеткой.
- Quiz: обновлён текст согласия на обработку данных + кнопка отправки теперь “Отправить заявку тренеру”.

## 37
### Исправления
- Шапка:
  - Починил «мозаику» в фоне шапки: `.bg-hero:before` снова использует `background-size: cover` и `background-repeat: no-repeat`.
- Quiz:
  - Починил кликабельность выбора возраста/района: `openPicker()` теперь корректно находит `#quizPickerTitle` и `#quizPickerOptions` (раньше искал классы, которых нет).
  - Убрал несуществующий `setAnswer()` из пикера (используется уже существующая логика через `setPickerValue()` по делегированию кликов).
  - На десктопе пикеры возраста/района показываются сеткой (без «одного пункта со скроллом»).

## 38
### Исправления
- Quiz (десктоп):
  - Пикер возраста: увеличил вместимость (сетку) — теперь варианты не обрезаются.
  - Пикер районов: сделал более компактную раскладку (3 колонки) — список влезает корректнее.
- Quiz (мобильная):
  - Вернул юридическое предупреждение про обработку персональных данных (теперь показывается и на мобильных, маленьким текстом внизу).
- Фон «тела» сайта:
  - Убрал принудительный `background-size: cover` — фон больше не «приближается/размывается», используется управляемый размер через `--site-bg-size-*`.

## 39

### Изменения
- Исправлены слетевшие шрифты/цвета: добавлены алиасы CSS-переменных `--bg`, `--text`, `--text-muted` и `--font-sans` (теперь текст снова светлый, а шрифт — sans-serif).
- Квиз (десктоп): выбор района стал компактнее (4 колонки + меньше padding/шрифт), чтобы все районы вмещались без обрезаний.
- Внутренние страницы (Локации/Отзывы/Буллинг/Сборы/Кабинет/и др.): убран фон «bg-hero» в шапке, теперь используется только фон тела (трава) — выглядит чище.

## 40
- Quiz (desktop): выбор возраста/района открывается поверх модалки (может выходить за границы), сетка в 2 колонки.
- Quiz: порог desktop для picker = 992px (на планшетах остаётся мобильное поведение).

## 50
- Главная: убраны блоки «Расписание/Локации» (карточки в цене), «Отзывы», «Программа», «Готовы начать». Добавлены блоки «Фото/Видео» (две карточки со стрелками) и финальный блок «Тестирование по запатентованной авторской методике».
- Навигация: добавлены пункты «Расписание» и «Контакты», приведён порядок ссылок. «Контакты» ведёт к футеру (#contacts).

## 51
### Полировка UX + расписание (демо-данные)
- Карточки/окна: убран «подпрыгивающий» hover-эффект — карточки больше не двигаются при наведении.
- Hero (mobile): скрыт длинный текст, кнопки перенесены под изображение мальчиков.
- Блок «Тестирование»: переделан в **одну** карточку со слайдером (стрелки + подпись).
- Медиа-блок «Фото/Видео»: стрелки листания выровнены по вертикали по центру карточек.
- Тренеры/Выпускники (desktop): добавлено пролистывание стрелками (snap-scroll карусель).
- Футер: уменьшена вертикальная высота, адреса разложены в 2 колонки, уплотнена мобильная версия.
- Расписание: временно забито данными (Новосибирск + Тольятти) в `assets/data/schedule.flat.json`.
- Расписание: добавлен справочник `assets/data/schedule.meta.json` (город→район→филиал) и логика зависимых фильтров в `assets/js/schedule.js`.



## 53
### Исправления (расписание/мультигород)
- Расписание:
  - Карточки занятий: контент больше не «вылазит» за пределы карточек (обрезка overflow + корректное сжатие pill-метаданных).
  - Колонки дней: перестали растягиваться до высоты самого длинного дня (короче дни заканчиваются под последней записью).
  - Бейджи количества: 1 цифра теперь отображается кружком (для 10+ добавляется класс `is-wide`).
- Футер: убраны адреса (чтобы не путать клиентов при нескольких городах).
- Квиз: добавлен шаг выбора города (первым), районы зависят от города; выбор района недоступен, пока город не выбран.
- Форма заявки (модалка «Записаться»): добавлен выбор города + зависимый список районов.


## 54 (2026-02-13)
### Исправления
- Расписание (desktop): карточка занятия в 7-колоночной сетке теперь в одноколоночной раскладке (время сверху), чтобы направление/возраст/район не уезжали за пределы карточки.
- Расписание (desktop): колонки недели не растягиваются по высоте самого длинного дня (align-items: start + fit-content).
- Блок «Примеры карточек тестирования»: изображения больше не обрезаются (height:auto вместо фиксированной высоты + cover); карточка подстраивается под размер картинки.
- Выпускники/Тренеры (mobile): фото больше не подрезаются по высоте (aspect-ratio: 4/5 + object-fit: contain на mobile).


## 56 (2026-02-14)
### Локации + медиа + визуальная полировка
- Локации:
  - Карта переведена на **Yandex Constructor** (2 embed-кода: Новосибирск/Тольятти) + переключение по выбору города.
  - Упрощён верхний блок страницы (убраны лишние элементы/кнопки/картинка), оставлен акцент на выборе города/филиала.
- Фото/Видео и блок «Тестирование»:
  - Стрелки листания позиционируются относительно **preview-области** (media-stage), визуально по центру.
  - Добавлена поддержка VK embed (используется `type: "vk"` + `src: https://vk.com/video_ext.php?...`).
  - Блок «Тестирование»: высота preview на desktop увеличена (clamp до ~450px).
- Главная:
  - В карточке «Наша философия» на desktop добавлено фото справа (mobile без изменений).
  - Иконка в блоке «Шахматы + футбол» заменена на кастомную мини-«шахматную доску» (без зависимости от Bootstrap Icons).
- Program:
  - Кнопки-табы в `program.html` стилизованы под тёмно‑зелёный glass (убран bootstrap‑blue).


## 57 (2026-02-14)
### Исправления
- Шахматы (главная): иконка «Шахматы + футбол» заменена на шахматную фигуру (♟) вместо «шахматной доски».
- Видео (VK/YouTube embed):
  - Отключено автозапускание по умолчанию (если в ссылке есть `autoplay=1`, автоматически нормализуется в `autoplay=0`).
  - В embed-плеерах убран `autoplay` из `allow`, чтобы не стартовали сами при прокрутке.
- Наша философия (главная): фото на desktop корректно расположено **справа**, верстка списка больше не «ломается».
- Program (program.html): табы больше не bootstrap-blue — добавлен класс `program-tabs`, стили из `theme.css` применяются.
- Расписание (schedule.html): добавлен класс `page` для правильного прижатия футера к низу, когда расписание пустое/город не выбран.
- Расписание (desktop): улучшена компоновка метаданных (направление/возраст/район) в узких колонках — вывод в столбик + ellipsis.
- Тестирование (главная): высота preview-области увеличена примерно на 50% (clamp 480–675px) без обрезки изображения.
- Контакты MAX: убран fallback на WhatsApp — MAX больше не ведёт на WhatsApp-ссылку (ссылка берётся только из `socials.maxLink`/`contacts.maxLink`).


## 58 (2026-02-15)
### Медиа/отзывы/локации — финальная полировка
- Видео VK: добавлен параметр `loop=1` по умолчанию для embed (уменьшает «предложение другого видео» после окончания ролика). Отключение — `"loop": false` у конкретного видео в `gallery.json`.
- Фото/Видео (главная): переработан рендер слайдов — превью теперь в контейнере `.media-stage` (aspect-ratio 16:9), фото **не обрезаются** (object-fit: contain), подпись вынесена из `.media-frame` (стрелки всегда по центру превью).
- Тестирование (главная): превью реально стало больше (JS теперь рендерит `.media-stage`, CSS задаёт высоту clamp 560–820px на desktop), подпись вынесена ниже превью.
- Шахматы + футбол (главная): иконка заменена на шахматного коня (♞).
- Наша философия (главная): увеличено место под текст (колонки 8/4), фото больше не режется (contain + aspect-ratio).
- Reviews:
  - Исправлены стили карточек отзывов (кнопка `.review-btn` больше не сбрасывает фон `card-dark`).
  - Добавлен новый блок **«Видео отзывы»** со слайдером (данные в `assets/data/gallery.json` → `reviewVideoSlides`).
  - Убрана «Фото и видео» галерея и лишняя картинка `assets/img/gallery/04.svg` из шапки.
- Locations: убран бейдж «Локации» и поясняющий текст в шапке — элементы страницы поднялись выше к заголовку.


## 59 (2026-02-15)
### VK видео / Отзывы / Локации / Тестирование
- Шахматы + футбол (главная): иконка заменена на шахматного **ферзя** (♛).
- VK видео (главная + reviews):
  - Убран бесконечный повтор по умолчанию (`loop` теперь выключен).
  - Добавлен `assets/js/vk-embed.js`: подключает VK.VideoPlayer JS API и **останавливает видео чуть до конца**, чтобы VK не показывал экран рекомендаций.
  - `gallery.json`: пример VK-видео обновлён (`"loop": false`).
- Reviews (reviews.html):
  - Блок **«Видео отзывы»** перемещён выше блока «Отзывы».
  - Карточка «Видео отзывы» сделана компактнее на desktop.
  - Отключено автолистание отзывов (только ручные стрелки/свайп).
  - На mobile показывается **по 1 отзыву на слайд**.
  - «Примеры траекторий» теперь визуально как «Выпускники» на главной (desktop snap‑карусель + mobile carousel).
- Тестирование (главная): превью подстраивает `aspect-ratio` под реальную картинку → фото визуально больше (меньше пустых полей, без обрезки).
- Локации: Yandex Constructor переключатели карт обновлены на `scroll=false` (карта не “улетает” от случайных жестов; zoom только +/−).


## 60 (2026-02-15)
### Reviews / Родителям: Буллинг / Локации / Программа / Тестирование
- Reviews (reviews.html):
  - Убраны лишние элементы в шапке (pill + CTA), шапка стала компактнее.
  - Карточка «Видео отзывы» на desktop сделана умеренно компактной (≈ -20% по ширине), но не «в 2 раза».
  - Отключено автоперелистывание отзывов (Bootstrap interval выключен).
  - На mobile гарантированно **1 отзыв на слайд** (устойчивое определение ширины viewport + перерасчёт при resize).
- Буллинг (parents-bullying.html): полностью переработана страница — вместо заглушек добавлены секции «текст слева + фото справа» (как блок «Шахматы» на главной).
- Локации (locations.html):
  - Убран лишний текст/карточки, на mobile вместо большого блока — компактная кнопка «Расписание».
  - Карта сделана более «резиновой»: `width=100%` в embed, увеличена высота на mobile.
  - Подняты элементы ближе к заголовку (уменьшены вертикальные отступы страницы).
- Тестирование (главная): увеличена реальная площадь превью (шире media-колонка + больше max-width). Убрано принудительное `height:auto`, которое делало превью слишком маленьким для широких скриншотов.
- Program (program.html): блок «Тренеры» теперь использует те же карточки, что на главной (данные из `assets/js/data.js`), на desktop — слайдер по 3 карточки, на mobile — по 1. Удалён нижний CTA-блок.


## 61 (2026-02-15)
### Тестирование (главная): desktop-only увеличение + аккуратные скругления
- Тестирование: исправлено поведение превью, чтобы **на desktop фото выглядело крупно без “пустых полос” сверху/снизу**.
- Mobile: блок тестирования снова компактный (высота stage возвращена к ~260px), без лишнего “раздувания”.
- `assets/js/media-cards.js`: добавлена подстройка `aspect-ratio` под реальную картинку **только на desktop** (картинка не обрезается, скругления выглядят правильно).


## 62 (2026-02-16)
### Тестирование (mobile) / Reviews / Hero
- Тестирование (главная): на mobile убран фиксированный `height` у превью → **картинка прилегает к рамке** без больших пустых полей (высота определяется `aspect-ratio`).
- `assets/js/media-cards.js`: подстройка `aspect-ratio` теперь работает и на mobile, но с более жёсткими ограничениями, чтобы блок не раздувался по высоте.
- Reviews (reviews.html): заголовок в шапке опущен чуть ниже (больше воздуха от меню).
- Reviews: удалён нижний CTA-блок «Хотите увидеть прогресс?» (по ТЗ).
- Главная (hero): убран верхний pill «3–16 лет • …»; левый блок hero поднят ближе к шапке на desktop.

